<%= javascript_tag do %>
  $(function() {
    <% if @search_results %>
      audiencias.globals.results = <%= raw @search_results.to_json %>;
    <% end %>
    audiencias.app.searcher();
    <% if @current_user %>
      audiencias.globals.currentUser = <%= raw @current_user.id %>;
      audiencias.globals.users.add(<%= raw @current_user.to_json %>);
    <% end %>
  });
<% end %>